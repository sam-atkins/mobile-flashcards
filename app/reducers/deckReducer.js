// import {

// } from '../actions/';

const initialState = {};

const decks = (state = initialState, action) => {

  switch (action.type) {
    default:
      return state;
  }
};

export default decks;
