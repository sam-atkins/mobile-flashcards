const initialState = {
  quizStatus: {
    toggleAnswer: false,
    toggleQuestion: true,
    toggleScore: false,
  },
};

const quiz = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default quiz;
